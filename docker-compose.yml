services:
  core:
    build: ./core
    profiles: [core]
    ports: ["8000:8000"]
    volumes:
      - "./vault:/vault"
      - "qmd_cache:/root/.cache/qmd"
    environment:
      - VAULT_PATH=/vault

  web:
    build: ./web
    profiles: [web]
    ports: ["5173:5173"]

volumes:
  qmd_cache: